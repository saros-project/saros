package de.fu_berlin.inf.dpp.test.invitation.internal;

public class InvitationProcessTest {

    /*
     * Der Invitation process wird vom xmpptransmitter aufgerufen, die logik der
     * fortlaufenden prozesses in innerhalb des invitation process gekapselt.
     * 
     * 1. Testen der Einzelfunktionalität. 1.1 Testen des empfangen einer Datei
     * durch einen inputstream
     */

    public void testRessourceReceived() {
	// IIncomingInvitationProcess process = new
	// IncomingInvitationProcess(transmitter, from,
	// projectName, description);
    }
}
