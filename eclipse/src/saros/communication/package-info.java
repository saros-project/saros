/**
 *
 *
 * <h1>Communication Overview</h1>
 *
 * The Communication package supports the communication of the participants of a Saros session via
 * VoIP or via the common chatroom (multi user chat) that is created for each Saros session.
 *
 * <h2>Subpackages</h2>
 *
 * <ul>
 *   <li>audio - component for realizing the Saros internal VoIP functionality
 *   <li>chat - component for realizing the Saros multi user chatroom (MUC) that is created for each
 *       Saros session and to which all participants of a session are automatically added, and the
 *       direct one-to-one chat (single) which is session-independent.
 * </ul>
 */
package saros.communication;
