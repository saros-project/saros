# Required metadata
sonar.projectKey=saros-server
sonar.projectName=Saros-Server
sonar.projectVersion=0.1.0

# Paths to source directories.
# Paths are relative to the sonar-project.properties file,  or to the base dir
# from which sonar-runner is run, if these are not the same. Replace "\" by "/" on Windows.
# Do not put the "sonar-project.properties" file in the same directory with the source code.
# (i.e. never set the "sonar.sources" property to ".")
sonar.sources=de.fu_berlin.inf.dpp.server/src
sonar.binaries=de.fu_berlin.inf.dpp.server/bin

# The value of the property must be the key of the language.
sonar.language=java

# Encoding of the source code
sonar.sourceEncoding=UTF-8

