/**
 * Renderer listen to the events that are fired by the core facades and convert them to JavaScript
 * events (using the model class). Renderer may also access core components directly, if they
 * provide a simple event mechanism.
 */
package saros.ui.renderer;
