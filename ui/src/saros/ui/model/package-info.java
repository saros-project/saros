/**
 * Provides classes for the ui model. These models are intended to represent domain objects as they
 * are needed for the presentation in the UI, e.g. it abstracts technical implementation details.
 * Models should not contain non-trivial logic, nor should they rely on other data types than
 * primitive types and other model classes.
 */
package saros.ui.model;
