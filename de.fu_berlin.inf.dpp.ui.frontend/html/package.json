{
  "dependencies": {
    "ampersand-app": "^1.0.0",
    "ampersand-collection": "^1.4.5",
    "ampersand-model": "^5.0.3",
    "ampersand-state": "^4.5.6",
    "ampersand-view": "^7.0.1",
    "backbone-events-standalone": "^0.2.6",
    "bootstrap": "^3.3.4",
    "domready": "^1.0.5",
    "jquery": "^2.1.3",
    "lodash.bind": "^3.1.0"
  },
  "devDependencies": {
    "browserify": "^9.0.3",
    "templatizer": "^1.3.0"
  },
  "scripts": {
    "build": "npm run build:scripts -s && npm run build:jade -s",
    "build:scripts": "browserify js/app.js -o bundle/bundle.js",
    "build:jade": "templatizer -d templates/ -o js/templates.js"
  }
}
